swagger:
  base-package: com.gulincover.home
  description: "index api development"
  group-name: "index api"
mybatis-plus:
  mapper-locations: classpath:mapper/*
spring:
  application:
    name: service-total
  cloud:
    nacos:
      discovery:
        server-addr: ${HOSTS:localhost:8848}